import type { Coordinate, Scene } from "../game/types";
import { GuessMap } from "./GuessMap";
import { PhotoViewer } from "./PhotoViewer";
type Props = { scene: Scene; round: number; total: number; guess: Coordinate | null; score: number; onGuess: (point: Coordinate) => void; onConfirm: () => void; onPhotoFailure: () => void };
export function RoundScreen({ scene, round, total, guess, score, onGuess, onConfirm, onPhotoFailure }: Props) { return <main className="game-screen"><header className="game-header"><a className="wordmark" href="/">GE<span>O</span>SAKA</a><div className="round-chip">ROUND <b>{String(round).padStart(2, "0")}</b> / {String(total).padStart(2, "0")}</div><div className="score-chip"><small>RUNNING SCORE</small><b>{score.toLocaleString()}</b></div></header><section className="round-grid"><div className="scene-column"><div className="scene-label"><span>LIVE PHOTO / OSAKA PREF.</span><i>{scene.difficulty}</i></div><PhotoViewer src={scene.display.imageUrl} alt={scene.display.alt} onFailure={onPhotoFailure} /></div><aside className="guess-panel"><div><p className="panel-kicker">PINPOINT THE PLACE</p><h2>ここは、<br /><em>どこや？</em></h2><p className="hint">地図をタップして、あなたの答えを置いてください。</p></div><GuessMap guess={guess} onGuess={onGuess} /><p className={`pin-status ${guess ? "placed" : ""}`}>{guess ? "ピンを置きました。これで勝負しますか？" : "まだピンがありません"}</p><button className="primary-button confirm" disabled={!guess} onClick={onConfirm}>推測を確定する <b>↗</b></button></aside></section></main>; }
