import type { RoundResult } from "../game/types";
import { formatDistance } from "../utils/formatDistance";
import { ResultMap } from "./ResultMap";
import { StreetViewLink } from "./StreetViewLink";
export function RoundResultScreen({ result, final, onNext }: { result: RoundResult; final: boolean; onNext: () => void }) { const { scene } = result; return <main className="result-screen"><header className="game-header"><a className="wordmark" href="/">GE<span>O</span>SAKA</a><div className="round-chip result-chip">ROUND {String(result.roundNumber).padStart(2, "0")} / 05 — ANSWER</div></header><section className="result-hero"><div className="result-score"><p>YOUR SCORE</p><strong>{result.score.toLocaleString()}</strong><span>POINTS</span></div><div className="distance"><span>距離</span><b>{formatDistance(result.distanceKm)}</b></div></section><ResultMap guess={result.guess} answer={result.answer} /><section className="reveal"><div className="reveal-number">0{result.roundNumber}</div><div><p className="panel-kicker">THE PLACE WAS</p><h1>{scene.reveal.nameJa}</h1><p>{scene.reveal.descriptionJa}</p></div><div className="credit"><b>PHOTO CREDIT</b><span>{scene.reveal.credit.author} / {scene.reveal.credit.licenseName}</span><a href={scene.reveal.credit.sourceUrl} target="_blank" rel="noreferrer">Wikimedia Commons ↗</a></div></section><StreetViewLink coordinate={result.answer} placeName={scene.reveal.nameJa} /><button className="primary-button next-button" onClick={onNext}>{final ? "最終結果を見る" : "次のラウンドへ"} <b>→</b></button></main>; }
